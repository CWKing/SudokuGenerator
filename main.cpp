#include "grid.h"
#include <iostream>

using std::cout; using std::endl;

void main() {
	grid newGrid;
	newGrid.printGrid();
	/*for (short i = 0; i < 81; ++i) {
		cout << (*newGrid.access)[i] << endl;
	};
	*/
};